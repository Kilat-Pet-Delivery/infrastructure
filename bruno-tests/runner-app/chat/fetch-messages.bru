meta {
  name: Fetch Messages
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/threads/{{threadId}}/messages
  body: none
  auth: bearer
}

params:query {
  page: 1
  limit: 50
}

auth:bearer {
  token: {{runnerToken}}
}
