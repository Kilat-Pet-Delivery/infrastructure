meta {
  name: Send Message
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/threads/{{threadId}}/messages
  body: json
  auth: bearer
}

auth:bearer {
  token: {{runnerToken}}
}

body:json {
  {
    "body": "On my way with your pet."
  }
}
