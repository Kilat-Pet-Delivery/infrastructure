meta {
  name: Complete Delivery
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/bookings/{{bookingId}}/complete
  body: json
  auth: bearer
}

auth:bearer {
  token: {{runnerToken}}
}

body:json {
  {}
}
