meta {
  name: Non Merchant 403
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/api/v1/shops/{{shopId}}/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{customerToken}}
}
body:json {
  { "status": "busy" }
}
