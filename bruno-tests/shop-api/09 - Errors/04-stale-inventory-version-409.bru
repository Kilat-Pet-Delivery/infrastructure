meta {
  name: Stale Inventory Version 409
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/v1/shops/{{shopId}}/products/{{productId}}/inventory
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ownerToken}}
}
body:json {
  { "quantity": 2, "reason": "adjustment", "expected_version": 0 }
}
