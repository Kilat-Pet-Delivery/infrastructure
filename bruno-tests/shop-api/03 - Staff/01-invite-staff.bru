meta {
  name: Invite Staff
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/shops/{{shopId}}/staff
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ownerToken}}
}

body:json {
  { "email": "shop-staff-smoke@example.com", "role": "staff" }
}
