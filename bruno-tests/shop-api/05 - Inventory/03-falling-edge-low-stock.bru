meta {
  name: Falling Edge Low Stock
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/api/v1/shops/{{shopId}}/products/{{productId}}/inventory
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ownerToken}}
}

body:json {
  { "quantity": 1, "reason": "sale", "expected_version": 1 }
}
